# OL~ WINE 기능 페이지 스크린샷 요청

`features.html`에는 현재 이미지 자리나 가상 화면을 넣지 않았다. 아래 실제 앱 화면을 받으면 기능 설명 옆에 맞춰 배치한다. 가능하면 원본 해상도의 PNG 화면 캡처를 제공한다. 화면 언어는 우선 한국어로 통일하고, 영문 페이지에도 영문 UI가 필요하면 같은 장면의 영어 설정 캡처를 추가한다.

공통 준비: 실사용 와인명, 구매처, 가격, 사진, 위치, 개인 메모 대신 공개해도 되는 예시 데이터를 사용한다. 알림·배터리 팝업·키보드가 화면을 가리지 않게 하고, 텍스트와 버튼이 잘리지 않은 세로 화면을 캡처한다. QR에는 실제 보관 데이터가 아닌 예시 와인을 사용한다.

## 우선 촬영할 화면

1. `cellar-layout.png` - 셀러 상세 화면에서 **선반형 층**을 선택한다. 종류가 다른 와인 2~3병, 빈 자리 1곳, `와인 자리 미사용 설정`으로 비활성화한 자리 1곳이 한 화면에 보이게 한다. 와인 종류별 아이콘과 자리 구분이 읽혀야 한다. 한 자리의 와인을 선택해 해당 와인이 목록에서 강조되는 장면까지 한 화면에 들어가면 가장 좋다. 들어가지 않으면 `cellar-layout.png`(자리 배치)와 `cellar-selection.png`(선택한 와인과 강조된 목록 행) 두 장으로 나눈다. **기능 페이지: 셀러 상세 보기와 위치 이동.**
2. `partial-tasting.png` - 보유 와인에서 시음기록을 입력하는 화면. 예시로 750ml 병에서 150ml를 마시는 상태를 만들고, 시음일·마신 양·평점이 보이게 한다. 메모나 사진은 예시 데이터만 사용한다. 가능하면 저장 후 600ml가 남은 와인 상세 화면을 `remaining-wine.png`로 한 장 더 제공한다. **기능 페이지: 시음 기록과 잔량.**
3. `wine-search.png` - 검색 화면에서 서로 다른 빈티지/병 크기의 결과가 여러 행 보이게 한다. `개별/와인별 보기`와 검색 조건이 보이는 상태를 우선 촬영한다. 필터 조건과 결과를 한 화면에 담기 어려우면 `wine-search-filters.png`(조건 선택)와 `wine-search.png`(적용된 결과)로 나눈다. **기능 페이지: 검색과 시음 이력.**
4. `ai-copy-single.png` - 와인 등록 또는 수정 화면에서 예시 와인명·빈티지와 AI 스크립트 복사 아이콘이 함께 보이게 한다. `ai-copy-selection.png`에는 셀러 상세 또는 검색 화면에서 와인 2~3개를 선택해 `N개 + AI 아이콘`이 보이는 장면을 담는다. 실제 AI 서비스 화면이나 AI가 작성한 답변은 필요하지 않다. **기능 페이지: 원하는 AI에 질문하기.**
5. `qr-label.png` - 프리미엄 QR 생성 화면에서 예시 와인용 라벨 또는 인쇄 미리보기가 읽히게 한다. 가능하면 스캔 후 관련 와인 화면으로 이동한 장면을 `qr-scan-result.png`로 추가한다. 실제 사용 중인 QR 코드는 노출하지 않는다. **기능 페이지: QR 라벨과 스캔.**
6. `manual-backup.png` - 프리미엄 수동 백업/복구 화면에서 사진 포함 선택과 백업·복구 명령이 함께 보이게 한다. 파일명, 개인 경로, 시스템 파일 선택 팝업은 보이지 않게 한다. **기능 페이지: 수동 백업과 복구.**

## 있으면 좋은 추가 화면

7. `cellar-move-before.png`, `cellar-move-after.png` - 같은 선반형 층에서 와인 한 병을 빈 자리로 옮기거나 두 병의 자리를 맞바꾸기 전후를 같은 확대율로 촬영한다. 드래그 중의 목표 자리 강조가 잘 보이면 `cellar-move-drag.png` 한 장을 대신 제공해도 된다.
8. `wishlist.png` - 구매희망 와인 2~3개와 수량이 보이는 목록. 실제 구매처나 가격 대신 예시 값을 사용한다. 구매완료 처리 중인 화면이 있으면 `wishlist-purchase.png`로 추가한다.
9. `cellar-overview.png` - 홈의 셀러 목록과 보관 병 수가 보이는 화면. 여러 셀러를 보여주려면 프리미엄 상태에서 예시 셀러 두 개 이상을 준비한다. 프리미엄 수량 제한 해제 설명에 사용할 수 있다.

캡처를 받은 후에는 해당 장면을 실제 기능 설명 옆에 추가하고, 이미지 대체 텍스트에도 화면에서 확인되는 상태만 적는다.
